import { profileReducer, profileActions } from 'entities/Profile/slice/profileSlice';

import { IProfile, IProfileSchema } from './types/types';

export { IProfile, IProfileSchema, profileReducer, profileActions };
